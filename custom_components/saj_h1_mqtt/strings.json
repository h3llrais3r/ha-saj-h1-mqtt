{
  "config": {
    "step": {
      "user": {
        "data": {
          "serial_number": "Serial number",
          "scan_interval": "Realtime data scan interval"
        },
        "title": "SAJ H1 inverter over MQTT"
      }
    },
    "abort": {
      "already_configured": "This device is already configured"
    }
  },
  "options": {
    "step": {
      "init": {
        "data": {
          "scan_interval": "Realtime data scan interval",
          "scan_interval_inverter_info": "Inverter info can interval (0=disabled)",
          "scan_interval_battery_info": "Battery info scan interval (0=disabled)",
          "scan_interval_battery_controller": "Battery controller scan interval (0=disabled)",
          "scan_interval_config": "Config scan interval (0=disabled)",
          "enable_mqtt_debug": "Enable MQTT debugging"
        },
        "title": "SAJ H1 inverter options"
      }
    }
  }
}
